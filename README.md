# SAILVu updates

This public repository is the read-only distribution channel for approved
SAILVu boat updates. It contains versioned application files and SHA-256
checksums used by SAILVu's built-in updater.

It intentionally contains no voyage records, instrument data, credentials,
private source history, or feedback packages from the boat.

The active stable release is described by `stable.json`.
